def connect_to_nearest_speaker():
    """
    Giả lập hành động quét và kết nối đến loa Bluetooth gần nhất.
    """
    print("Đang quét các thiết bị Bluetooth ở gần...")
    # Giả lập thời gian quét
    import time
    time.sleep(2)
    print("Đã tìm thấy loa 'JBL Go 3'. Đang tiến hành kết nối...")
    time.sleep(1)

    # Đây là nơi logic kết nối thật sẽ được thêm vào sau này

    print("Đã mô phỏng kết nối thành công đến loa 'JBL Go 3'.")
    return True
